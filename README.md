# Synthetic merge-queue fixture

This repository contains deliberately synthetic code used only to compare merge
queue behavior. It contains no Orient One source, configuration, history,
customer data, or credentials.

The stable required workflow job is `fixture-ci`.
